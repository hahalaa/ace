[tool.pytest.ini_options]
# src/ on the import path so tests use the same src-relative imports as the
# runtime pipeline (`import config`, `import data.loader`) — no per-file
# sys.path hacks (T0.5). Runtime deps are pinned in requirements.txt.
pythonpath = ["src"]
testpaths = ["tests"]
