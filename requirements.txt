# Baseline dependencies for the classifier pipeline (Phase 0, T0.5).
# FastAPI/uvicorn/pydantic are added in Phase 3 (T3.1); frontend deps live in frontend/package.json (Phase 4).
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
matplotlib>=3.7
seaborn>=0.12
joblib>=1.3
pytest>=7.4
