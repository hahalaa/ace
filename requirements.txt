# Python deps for the model/sim/API. Frontend deps are in frontend/package.json.
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
matplotlib>=3.7
seaborn>=0.12
joblib>=1.3
pytest>=7.4

# Pinned to a tested range: these have shipped breaking changes in minor releases.
fastapi>=0.115,<0.117
uvicorn[standard]>=0.34,<0.36
pydantic>=2.9,<3  # v2 required: src/api/schemas.py uses v2-style schemas.
httpx>=0.27,<0.29  # test-only: fastapi.testclient's transport.

# Rate limiting needs no package: in-memory sliding window in api/main.py.
