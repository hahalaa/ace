# Baseline dependencies for the classifier pipeline (Phase 0).
# Frontend deps live in frontend/package.json (Phase 4).
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
matplotlib>=3.7
seaborn>=0.12
joblib>=1.3
pytest>=7.4

# API (Phase 3). FastAPI/Starlette are pinned to a tested range rather than
# latest because they have shipped breaking changes in minor releases. pydantic
# is held to v2 (the schemas in src/api/schemas.py are v2-style).
fastapi>=0.115,<0.117
uvicorn[standard]>=0.34,<0.36
pydantic>=2.9,<3
# TestClient's transport; a test-only dependency of fastapi.testclient.
httpx>=0.27,<0.29
# /storybook rate limiting needs no dependency: it is a small in-memory sliding
# window in api/main.py (see config.API_STORYBOOK_RATE_LIMIT).
