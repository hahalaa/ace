# Baseline dependencies for the classifier pipeline (Phase 0).
# Frontend deps live in frontend/package.json (Phase 4).
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
matplotlib>=3.7
seaborn>=0.12
joblib>=1.3
pytest>=7.4

# API (Phase 3). Upper bounds are deliberate: FastAPI/Starlette move fast
# and have shipped breaking changes in minor releases, so the API is pinned to a
# tested range rather than tracking latest. pydantic is held to v2, the schemas
# in src/api/schemas.py are v2-style and v1 is a different library in practice.
fastapi>=0.115,<0.117
uvicorn[standard]>=0.34,<0.36
pydantic>=2.9,<3
# TestClient's transport; a test-only dependency of fastapi.testclient.
httpx>=0.27,<0.29
# NB: rate limiting for /storybook needs no dependency, it is a tiny in-memory
# sliding window in api/main.py (SlidingWindowRateLimiter). See
# config.API_STORYBOOK_RATE_LIMIT for its deliberately narrow scope.
