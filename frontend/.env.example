# Copy to .env (gitignored); the client throws ApiConfigError naming this file if VITE_API_BASE_URL is unset.
# The origin must also be in config.API_ALLOWED_ORIGINS on the API side.
VITE_API_BASE_URL=http://localhost:8000
